# Pinned to the versions the model was verified against. scikit-learn in
# particular is pinned because pickled RandomForest models can fail to load
# across major library versions.
streamlit>=1.32,<2
opencv-python>=4.8,<5
mediapipe>=0.9,<0.11
numpy>=1.24,<2
scikit-learn==1.2.0
